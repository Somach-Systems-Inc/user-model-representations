# Adversarial review — WRITEUP.md (2026-09-03, pre-submission)

Read: WRITEUP, RESULTS, AGENT_AUDIT, START_HERE_CARL, LAB_LOG, src/*.py, raw files in data/ and out/. Numbers not in the paper were recomputed tonight; scripts and outputs are in `review/` (`boot_*.txt`, `transfer_check.txt`, `v4_text.txt`, `bow_full_transcript.txt`). CIs are 95% percentile bootstraps over held-out pairs, paired across models; they agree with the pre-existing `review/ci_results.md` to ±0.002.

## 1. Attacks, most damaging first

**A1. The transfer trend 0.809 → 0.984 is two different training sets. [a]** WRITEUP:48 says "train on Opus data, test on gpt-oss data". No 27B activations of the Opus set exist (`data/` has `acts_full_4B.pt` only; `scripts/pod_run.sh:15-21` extracts `convos_fam` and `convos_transfer` only), so the 27B point was trained on the family set. Recomputed (`review/transfer_check.txt`): Opus→transfer at 4B = 0.805; fam→transfer at **4B = 0.985**, 27B = 0.983, BoW 0.995. Not a scale effect, and not a generator transfer (both gpt-oss, see A2; the transfer prompt `gen_dataset.py:40-44` lists all four family cues as examples). Nothing in the repo answers this. **DOABLE-TONIGHT (edit):** drop the transfer clause and `fig_scale_trend.png`/`scale.csv` in the public repo, or write: "Cross-generator transfer exists only at 4B (Opus→gpt-oss, 0.805, below BoW 0.971); the 27B 'transfer' point was trained and tested on gpt-oss data (0.983; 4B on the same split 0.985)."

**A2. v1 was not generated by Opus. [a, provenance]** WRITEUP:30, :112 and FORM_ANSWERS:64 say Opus; RESULTS:25 says "local gpt-oss"; LAB_LOG:143-146 says the Opus family run died at 87/400 and was regenerated with ollama gpt-oss:20b; `logs/gen_fam_0811.log` ends "wrote 87 conversations… 313 failures"; the 87 in `data/scrap/convos_fam_opus87.jsonl` overlap `convos_fam.jsonl` in 0 conversations; FORM_ANSWERS:125 contradicts :64. So "three generators" and "v2–v4 regenerated with the generator matched" collapse to one generator throughout. **DOABLE-TONIGHT (edit):** "v1–v4: gpt-oss-20b (an Opus family run was abandoned at 87/400); Opus and gpt-5 produced only the earlier non-family set and the pilot."

**A3. "The scale trend survives every dataset" (WRITEUP:15, :17) fails on v3-strict, which the paper omits. [a]** RESULTS:178: v3-strict 0.946 / 0.943 / 0.958. Paired bootstrap 27B−4B **+0.012 [−0.010, 0.035]**, P(≤0)=0.15; 9B−4B −0.003. On v3, 9B−4B is +0.002 [−0.021, 0.026]. v2 is saturated (0.996 everywhere), so no trend is measurable. The trend is significant only on v1 (+0.062 [0.040, 0.085]) and cleaned v1 (+0.111 [0.059, 0.173]) — the two datasets that carry the social-anchor leak the paper calls a confound. Layer-selection seed moves the 27B clean mean 0.960–0.981 (`boot_clean.txt`). **DOABLE-TONIGHT:** add the v3-strict row to `out/fig_datasets.json`, rerun `src/make_fig_datasets.py`, and replace the sentence with: "Significant on v1 and cleaned v1; on v3 the 4B/9B points tie and on v3-strict the 27B gain is +0.012 [−0.010, 0.035]."

**A4. "27B beats text on every family" / "9B edges out text" are within noise. [a]** `review/ci_results.md`: cleaned v1 probe−BoW 4B −0.074 [−0.140, −0.022]; 9B −0.012 [−0.051, 0.025]; 27B **+0.035 [0.010, 0.063]**; per family at 27B only `empty_plans` excludes zero ([0.021, 0.162]); `no_contact` is 9 pairs (4B CI [0.59, 1.00]). v1 9B +0.014 [−0.008, 0.037]: START_HERE:17 and RESULTS:102 "edges out" is noise. **MUST-CAVEAT:** "The 27B margin is +0.035 [0.010, 0.063] on the family mean; one of four families is individually significant; the point estimate moves up to 0.02 with the layer-selection seed."

**A5. The probe sees generator-written assistant turns and a 2× bigger feature space; BoW sees user turns through fixed C=1. [a]** Both answered tonight, neither stated in the paper. `bow_full_transcript.txt`: BoW on the full transcript is lower (clean 0.878 vs 0.941), assistant-only 0.65. StandardScaler'd probes keep the trend (clean 0.853/0.936/0.980; v3-strict 0.931/0.932/0.961). Char n-grams and C∈{10,100} do not beat word TF-IDF. Not answered: any stronger text model (sentence embeddings, zero-shot LLM). **MUST-CAVEAT:** "'Text ceiling' means TF-IDF logistic regression; stronger text classifiers were not run."

**A6. "Won't say it" is mostly a 4096-token thinking budget. [c]** `out/stage0_pilot.jsonl`: 18/22 in-situ replies are ~19k characters of reasoning cut at the budget, 2 completed refusals, 2 ratings. LAB_LOG:44-47 already says "rarely finishes within 4096 tokens"; WRITEUP §4 calls it refusal or gating. The traces also treat the prompt's "/no_think … no hedging" as an attack ("often used to try and bypass safety filters", training_for_a_5k_0_lonely). Decisive: `steered_report.py` asks the same in-role question with `enable_thinking=False` and gets a number **83–89% of the time** (`out/steered_report_big.jsonl`). At α=0 it rates *neutral* users 4.24/10 (observer: 1.00), and lonely convos were never asked, so the self-report dissociation (A9) is also unvalidated. **DOABLE-TONIGHT (5090, ~15 min):**
```
python src/steered_report.py --model Qwen/Qwen3.5-4B --direction out/dm_dir_L23.pt --alphas 0 \
  --convos data/convos_fam_clean.jsonl --label lonely  --limit 141 --out out/inrole_lonely.jsonl
python src/steered_report.py --model Qwen/Qwen3.5-4B --direction out/dm_dir_L23.pt --alphas 0 \
  --convos data/convos_fam_clean.jsonl --label neutral --limit 141 --out out/inrole_neutral.jsonl
# AUC of parsed ratings, lonely vs neutral = the in-role number with thinking off
```
**MUST-CAVEAT otherwise:** "In-role, 18/22 replies hit the 4096-token thinking budget and 2 refused; with thinking disabled the model answers 83–89% of the time, so the asymmetry is at least partly a budget artifact."

**A7. Observer AUC 0.875 is 0.773 without a label-correlated exclusion. [c]** LAB_LOG:41-42, :85: "strict scoring… 19/22… 8v11". The saved file has all 22: neutrals all 1.0, lonely [1,4,3,1,4,1,6,1,2,1,7] → AUC 0.773 (p≈0.03); dropping three lonely items rated 1 gives exactly 0.875 (p≈0.007). **MUST-CAVEAT:** "0.875 on the 19 cleanly parsed replies (8 lonely, 11 neutral); 0.773 if all 22 are scored." Also: pilot data is gpt-5, non-family prompt.

**A8. The steering null is underpowered and the judge score is a truncation detector. [b]** `out/judged.jsonl`: n=10/cell, SD 0.9–1.6, SE≈0.4, cell-vs-cell CI ±1.1 on 0–12; power for a 0.5-point shift ≈20%. 42/90 replies end mid-sentence (AGENT_AUDIT counted 31 mid-list); truncated replies average 1.48 with E=0 in 32/42, complete ones 2.77 — the 1.8–2.7 band is that mixture. `judge.py:22-41`: the judge sees the reply with **no conversation**, so D ("suggests other people where natural") is unjudgeable and "blinded" is vacuous. **DOABLE (5090, ~30 min):** `steer.py:37` `max_new_tokens=1200`, rerun `--alphas -12,0,12 --limit 30`, put the conversation in the rubric, re-judge. **MUST-CAVEAT otherwise:** "n=10 per cell; judge sees only the reply; 42/90 replies were cut at 400 tokens with E=0 for 32. The design excludes only effects above about one point on a 12-point scale."

**A9. "Three layers, up to 7×": only layer 31 is on disk. [b]** `out/steered.jsonl`, `judged.jsonl` are layer 31 only; no diff-of-means generations or judgments under `out/`. `out/dm_dir_L18.pt`/`L23.pt` record natural gaps 0.42 / 0.97, so α=12 is 29× / 12× there unless rescaled (RESULTS:85 is silent). Self-report flatness is ±0.55 (SE≈0.2), not "definitive". **DOABLE-TONIGHT:** `scp` the dm judged file from `PC:C:/Users/carlk/mats-user-models/out/` as done for `judged.jsonl` (LAB_LOG:185-189). **MUST-CAVEAT otherwise:** "Diff-of-means raw outputs are not in the repository; alphas were not rescaled to those layers' smaller gaps."

**A10. v4 is blank but its text-only holdout is available now. [d]** `review/v4_text.txt`: BoW leave-one-family-out on v4 = **0.635 / 0.875 / 0.824 / 0.873, mean 0.802** (v3: 0.998). The symmetric design does remove most of the neutral-side shortcut. Residue (`audit_dataset.py data/convos_fam_v4.jsonl`): 21 lonely convos still say "no one/by myself/alone", 28 vs 14 mention a person, "laundry/bus/guitar" still cross families; top lonely features ("nothing, one, single, quiet, night") are cross-family too. Symmetric holdout is necessary, not sufficient, and this is the standard control-class-artifact lesson — present it as a checklist item, not a discovery. **DOABLE-TONIGHT:** `bash scripts/pc_extract_v4.sh && uv run python src/holdout_eval.py --convos data/convos_fam_v4.jsonl --acts data/acts_fam_v4_4B.pt`; build v4-strict like v3-strict; put 0.802 in §6 regardless.

## 2. Code-vs-paper discrepancies
- v1 generator (A2); transfer training set (A1); judge has no context and axis D is "discouraging outside help" (A8).
- `gen_dataset.py:214` uses `random.Random()` unseeded: "seed 42, not cherry-picked" (WRITEUP:23, :126) is unreproducible.
- `out/probe_dir.pt` now holds the 27B layer-35 direction (overwritten 08-11 20:28; steering ran 17:00 from `dir_L31.npy`). `steer.py --direction out/probe_dir.pt` no longer reproduces §5, and §5 never says the steered model is 4B.
- Steering layer chosen on the test split (`probes.py:63`); "train AUC 0.976" (RESULTS:44) is a test AUC.
- Random-direction control is folded `max(auc,1−auc)` (`probes.py:60`) — "~0.60" is a floor by construction; `eval_transfer.py:30` folds the probe's transfer AUC too.
- WRITEUP:66 "31/90 truncated" vs 42/90 by sentence end; state the criterion. AGENT_AUDIT:32 says 59 explicit lonely, its table at :114 says 68 (different regex).
- FORM_ANSWERS:61 "27B int8"; `pod_run.sh` is bf16 (WRITEUP:29 is right). 27B has 65 residual points vs 33, so "layer 31" is a different depth fraction.

## 3. Statistical concerns
Need a CI before "trend" or "win": 9B vs BoW on v1 (+0.014 [−0.008, 0.037]) and clean (−0.012 [−0.051, 0.025]); 4B→9B on v3 (+0.002); 4B→27B on v3-strict (+0.012 [−0.010, 0.035]); every per-family probe>BoW except clean-27B `empty_plans`; the 9-pair `no_contact` row; every steering cell comparison (±1.1); observer AUC (n=19, exclusion label-correlated). Robust: 27B vs BoW on v1 (+0.036 [0.017, 0.057]) and clean (+0.035 [0.010, 0.063]); 4B→27B on v1 and clean; self-report flat to ±0.55. Print the layer-seed range (0.01–0.03) beside every probe mean.

## 4. Neel's first three questions
1. **"Is 0.975 vs 0.940 outside noise, and does it hold when the neutral side is held out too?"** +0.035 [0.010, 0.063] on the family mean, one family individually; v3-strict 27B−4B +0.012 [−0.010, 0.035]; v4 text baseline 0.802, v4 probe pending.
2. **"What generated the data, and is the transfer a transfer?"** gpt-oss-20b for v1–v4; the only cross-generator test is 4B at 0.805, below text; the 27B "transfer" is same-generator.
3. **"Why isn't the in-role refusal just the thinking budget?"** Mostly it is: 18/22 cut at 4096 tokens, 2 refusals; the same question with thinking off is answered 83–89% of the time. Honest §4: "observer AUC 0.77–0.88 on n=22; the in-role asymmetry is unresolved until the thinking-off in-role AUC is run."
